import ProgressBar from "../components/ProgressBar";

export default function LaunchVentas() {
  return(
    <div>
      <h1>Launch ventas</h1>
      <ProgressBar activeStep={2} />
    </div>
  );
}