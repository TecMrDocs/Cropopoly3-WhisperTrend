import ProgressBar from "../components/ProgressBar";

export default function LaunchRegistroVentas() {
  return(
    <div>
      <h1>Launch registro ventas</h1>
      <ProgressBar activeStep={2} />
    </div>
  );
}