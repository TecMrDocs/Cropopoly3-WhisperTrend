import ProgressBar from "../components/ProgressBar";

export default function LaunchProducto() {
  return(
    <div>
      <h1>Launch producto</h1>
      <ProgressBar activeStep={1} />
    </div>
  );
}